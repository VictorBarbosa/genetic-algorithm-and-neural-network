import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'genetic-algorithm-and-neural-network',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
